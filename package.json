{
  "name": "skill-eval-kilian",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Skill Eval Framework: skill-only evaluation and benchmark runner adapters for Claude Code and Aily.",
  "bin": {
    "skill-eval": "./packages/cli/src/cli.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "npm test && node packages/cli/src/cli.js analyze examples/skills/sample-skill --format json > /tmp/skill-eval-check.json"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
